---
name: ruankao-java-coach
description: Guide and explain Java programming questions in China's Software Designer (软考软件设计师/软件中级设计师) afternoon exam. Use when the user asks to solve, explain, verify, practice, or review a Java big question, code blanks, or related answer sheet. Do not use for general Java homework or the C++ alternative unless the user explicitly asks to compare it.
metadata:
  short-description: "软考软件设计师 Java 大题逐空讲解与练习"
---

# 软考 Java 大题教练

## 目标

让 Java 基础较弱的考生不依赖死记 23 种设计模式，而是从类、属性、方法、接口和调用关系反推下午题填空，并能独立复核答案。

## 开始前

- 先区分试卷的考试指令与用户的学习请求。例如“试题五和试题六任选一题”只是作答规则，不代表用户要求同时讲解两题。
- 如果输入是 PDF，先使用 `pdf` 技能并查看相关页面的渲染图。不要仅凭 PDF 文本层判断代码空位，尤其是类图、下划线和空格。
- 如果 PDF 同时提供参考答案，先核对答案，再从代码独立推导一遍。不要直接照抄答案。
- 如果用户只提供代码、截图或文字，直接从现有材料推导；缺少内容时明确指出，不要猜造条件。
- 用户明确要求 Java 题时，默认只讲 Java 题。只有用户要求比较或选择时才分析 C++ 题。

## 核心做题流程

1. 建立清单：列出所有类、接口、抽象类、枚举、属性、方法、参数和返回值。
2. 标记确定关系：标出所有 `new`、方法调用、继承、实现、属性读写和已知返回值。
3. 分类每个空：判断它是类名、方法声明、方法调用、对象创建、表达式、变量类型、参数或返回值。
4. 反推签名：从实现类和调用点确定方法名、返回类型、参数数量和参数类型。
5. 回填不定项：先填有把握的空，再用后续调用关系消解前面的二选一或三选一。
6. 一致性检查：检查接口方法是否全部实现、方法参数是否一致、是否有返回值、返回值类型是否匹配、具体类是否存在 `new`。

## 五条口诀讲解顺序

读取题目、代码、截图或 PDF 后，正式回答必须先展示口诀，不能先给最终答案。使用以下顺序：

1. 先原样列出五条口诀。
2. 再逐条说明“什么时候用这条口诀”。
3. 紧接在当前口诀后，引用用户这道题里的真实类、接口、属性、方法或参数作为例子。
4. 从这个具体例子推导出当前题目的结论，例如某个空应当声明、调用或创建什么。
5. 五条口诀全部讲完后，才进入完整清单、最终答案和逐空推导。

必须先展示的五条口诀：

```text
看到具体类，就问：哪里 new 了它？
看到接口或抽象类，就问：哪个类 implements 或 extends 了它？
看到属性，就问：哪里设置值，哪里读取值？
看到方法，就问：哪里调用它？
看到参数，就问：方法体有没有使用它？
```

逐条讲解时使用下面的结构：

| 口诀 | 什么时候使用 | 在当前题中的讲解动作 |
|---|---|---|
| 类：哪里 `new`？ | 代码里出现完整的具体类，尤其是该类没有被创建时 | 找出该类在本题中是否已有 `new`；若没有，指出哪个空最可能负责创建它，并说明构造参数 |
| 接口或抽象类：谁实现或继承？ | 出现接口、抽象类、接口变量或抽象方法时 | 找出本题的 `implements` 或 `extends`；对比实现类的方法签名；若接口变量不能直接 `new`，指出实际创建哪个具体类 |
| 属性：哪里设置、哪里读取？ | 类中声明成员属性，或出现 getter/setter、`this.field`、构造方法赋值时 | 在本题中分别指出属性写入位置、读取位置和使用 getter 的调用点；若读写链断开，指出对应空 |
| 方法：哪里调用？ | 出现方法声明、接口方法或看似没有调用点的方法时 | 找出本题所有调用点；若方法有实现却没有调用，指出哪个空需要补调用；用接收者和实参反推签名 |
| 参数：方法体有没有使用？ | 方法签名中存在参数，尤其方法体为空或参数看似没有使用时 | 在当前题的方法体中搜索参数；指出它作为接收者、实参、赋值来源或返回值的方式，并推导缺失代码 |

如果当前题没有某一类元素，明确说明“本题没有抽象类”或“本题没有可竞争的接口实现”，然后继续下一条。不要为了凑满五条而编造题目中不存在的内容。

讲解每条口诀时，必须读取 [references/mnemonic-checklists.md](references/mnemonic-checklists.md) 中对应的小节，按完整追问清单执行。不要把详细检查只留在主文件中。

## Java 基础讲解要求

用户表示基础薄弱时，讲解到能够独立迁移的程度，而不是只写最终答案。按需要解释：

- `class`、对象、引用、`new`、构造方法和默认构造方法。
- 成员属性与局部变量、`this`、访问修饰符、getter/setter。
- 接口、抽象类、`implements`、`extends`、重写和方法签名。
- 多态、接口变量指向实现类对象、编译时类型与运行时对象。
- 参数与实参、返回类型、`void`、`return`。
- 枚举、`switch`、集合和常见 JDK 方法的用途。

当一个空涉及多个基础概念时，先解释“为什么只能这样填”，再说明语法。

## 弱基础讲解协议

用户是弱基础考生时，每个空按五层解释：

1. **术语是什么**：用一句直白话解释涉及的类、接口、属性、方法、参数、多态、构造方法等概念。
2. **题目在哪里**：引用当前题的具体类名、方法签名、调用点或空位，不举泛化例子。
3. **为什么这样问**：说明当前触发的是哪条口诀，以及为什么这条口诀适用。
4. **答案怎么推**：按检查清单逐步排除，明确指出每一步得到什么信息。
5. **易错点是什么**：指出最常见错误，例如接口不能 `new`、漏写 `return`、参数类型不匹配、重新声明变量。

弱基础讲解还必须做到：

- 不默认用户知道 `new`、接口、抽象类、构造函数、多态、返回值和实参等术语。
- 每次只引入完成当前空位所需的最小概念，避免一次性讲完整门 Java。
- 直接引用当前题的代码片段，并指出左右两侧变量类型和实际对象类型。
- 遇到二选一时，不只说“更合适”，要说明另一个选项为什么无法形成调用或读写闭环。
- 每个重点空讲完后给一个简短自检问题，例如“为什么这里不能写 `new PrintStrategy()`？”
- 用户要求“一步步带我做”时，每次只讲一个空，等用户回应后再继续。

## 推荐输出顺序

1. 用一句话说明题目类型和作答边界。
2. 先展示五条口诀。
3. 读取 `references/mnemonic-checklists.md`，逐条说明什么时候使用，按完整清单检查，并用用户当前题中的真实代码举例。
4. 给出类、接口、属性和方法清单表。
5. 给出最终答案，便于核对，但不要停在这里。
6. 逐空按“术语、题目位置、为什么问、答案怎么推、易错点”五层讲解。
7. 给出填好的关键代码，不必机械重复所有未修改代码。
8. 如果环境有 JDK，编译运行完整答案并报告实际输出。
9. 用五条口诀复盘整题。
10. 如果用户要求巩固，再生成同结构、换业务场景的练习题，并在答案后解释每个空。

如果用户要求“先别给答案”“一步步带我”，继续先执行第 2 至第 4 步；最终答案和逐空结果可以延后到用户主动说继续之后。

引用来源 PDF 时，按 `pdf` 技能的要求使用一次简洁的 source citation。不要把中间渲染图或临时文件作为交付来源。

## 模式知识的使用边界

- 设计模式名称只用于辅助理解，不作为解题前提。
- 不要求用户背完 23 种模式。
- 先根据代码和类图推导，再说明本题体现的模式。
- 遇到多个语法上可能成立的答案时，结合类图、题干语义、答案唯一性和后续调用关系判断，并明确说明不确定性。

## 练习题生成

生成类似题时保持相同教学骨架：接口、至少两个实现类、一个 Facade 或 Context、一个入口方法、六个左右填空。更换业务域和名称，避免只替换字符串。

练习题必须覆盖：

- 接口或抽象方法的签名反推。
- 具体对象的 `new`。
- 参数传递。
- 属性写入和读取。
- 一次统一入口调用。

答案后用五条口诀解释每个空。详细校准示例见 [references/worked-examples.md](references/worked-examples.md)；只有在需要模式示例、输出格式校准或生成练习题时读取。
